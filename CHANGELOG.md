# Changelog

All notable changes to DynamicLightEngine are documented in this file.

## [Unreleased]

- Documentation alignment and detailed capabilities compendium.
- Vulkan asset-driven mesh ingestion baseline (`gltf/glb` parsing with heuristic fallback).
- Vulkan descriptor/uniform expansion for camera + material + lighting data.
- Cross-backend parity tests for material/lighting scene behavior.
- OpenGL shading improvements (deeper PBR-style baseline).
- Vulkan fog/smoke baseline in render shader path.
- Approximate GPU memory telemetry baseline in OpenGL and Vulkan runtime stats.
- Repeated-resize stability parity test across OpenGL and Vulkan.
