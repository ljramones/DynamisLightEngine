# Default fallback mock-Vulkan compare threshold overrides (CI safety only).
#
# Generated recommendations can be promoted here with:
#   ./scripts/promote_compare_thresholds.sh artifacts/compare/.../recommended-thresholds.properties mock
#
# Format:
#   threshold.<profile>=<decimal>
