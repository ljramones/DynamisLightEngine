/**
 * Render feature capability contracts for backend composition metadata.
 *
 * This package is intentionally metadata-only in v1. It does not perform
 * runtime wiring and does not alter backend behavior by itself.
 */
package org.dynamislight.spi.render;
